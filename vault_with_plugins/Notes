### PARA HTPGETTOKEN
He tenido que instalar para el htget apt-get install libkrb5-dev
apt-get install libssl-dev swig python3-dev gcc openssl 

y luego con pip3 pues instalar kerberos, M2Crypto y Openssl??

pip3 kerberos
pip3 install pyOpenSSL
pip3 install m2crypto

### DOCUMENTATION

En la docu poner la version de docker que se usa y de vault internar usar la no latest (una en concreto vaya)
Explicar como se han compilado los binarios como pone en la documentacion
Poner que se ha comentado toda la vaina de kerberos

Hay que poner en la docu tambien los comandos que se ejecutan con make para montarlo todo bien